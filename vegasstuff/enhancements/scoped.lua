SMODS.Enhancement{
    key = "scoped",
    atlas = "Scoped",
    pos = {x = 0, y = 0},

    loc_txt = {
        name = "Scoped",
        text = {
            "{C:inactive}???{}" -- will double the values on the card if held in hand at the end of round
        }
    },

    any_suit = false,
    replace_base_card = false,
    no_rank = false,
    no_suit = false,
    always_scores = false,
    unlocked = true,
    discovered = true,
    no_collection = false,
    weight = 5
}